#include <stdio.h>
int main()
{
    int a;
    scanf("%d",&a);
    if(a%2==0){
        if(a%4==0)
        {printf("the number is divisible by 4 and 2");}
    else
    {printf("the number is not divisible by 4");}
    }
    else{
        printf(" the number is not divisible by 2 and by 4");}
    }
    
        
    
    


